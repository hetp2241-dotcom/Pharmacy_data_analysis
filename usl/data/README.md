# Pharmacy Dataset

This workspace now contains only the pharmacy analytics dataset assets.

Files generated:

- `pharmacy_30k/medicine_master.csv`
- `pharmacy_30k/inventory_batches.csv`
- `pharmacy_30k/sales_transactions.csv`

The generator is:

- `../scripts/generate_pharmacy_dataset.py`

The sales transactions file is generated with exactly `30,000` records.
